# Tarefas de Refatoração Arquitetural


## 1. Resolução do Vazamento de Abstração do Zod
- [/] Atualizar `makeValidationSchemaMiddleware.js` para importar os schemas e passá-los ao provider.
- [/] Atualizar `ValidationSchemaMiddleware.js` para receber `schemaName` no execute.
- [/] Modificar `UserRouter.js` e `AuthRouter.js` para usar `schemaName` (string) em vez do objeto Zod importado.

## 2. Inversão Genuína no Error Handling (OCP)
- [/] Modificar `AppError.js` para suportar '`type`.
- [/] Atualizar as classes filhas de `AppError` com seus devidos `type`.
- [/] Simplificar `ErrorHandlerMiddleware.js` para usar `err.type`.

## 3. Refatoração de Value Objects Anêmicos
- [/] Modificar `PasswordValueObject.js` implementando `create` e `restore`.
- [/] Remover `alreadyHashed` do construtor de `UserEntity.js` e `PasswordValueObject.js`.
- [/] Atualizar os Use Cases (como `CreateUserUseCase`) para usar os Factory Methods estáticos vindos das entidades.

## 4. Modelo de segurança
- [/] Adicionar a comunicação de tokens via cookie, sem passar pelo body da aplicação
- [/] Verificar se o usuario tem uma sessão ativa antes de aprovar o login
## 5. Desacoplamentos
- [/]  Composition Root no server para desacoplar a classe envs do server


# Features / Refatoraçõess Futuras

## 1 Refatorações
- [ ] Refatorar entidades e use cases, garantir que a regra de negocio fique totalmente encapsulada na entidade
- [ ] Adicionar cache a side para busca de usuarios
- [ ] Adicionar acesstoken em uma blacklist durante o logoff
- [ ] Adicionar a busca por usuario a travez de req params